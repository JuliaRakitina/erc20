# Placeholder template for host-only development. No keys or credentials belong here.
# README contains the concrete, disposable local-demo setup.
NODE_ENV=REPLACE_WITH_development
LOCAL_DEMO=REPLACE_WITH_true
SIGNER_MODE=REPLACE_WITH_hardhat-local
SIGNER_ACCOUNT_INDEX=REPLACE_WITH_LOCAL_ACCOUNT_INDEX
CHAIN_ID=REPLACE_WITH_LOCAL_CHAIN_ID
RPC_URL=REPLACE_WITH_LOCAL_RPC_URL
DEPLOYMENT_FILE=REPLACE_WITH_DEPLOYMENT_JSON_PATH
HOST=REPLACE_WITH_LOOPBACK_HOST
PORT=REPLACE_WITH_API_PORT
